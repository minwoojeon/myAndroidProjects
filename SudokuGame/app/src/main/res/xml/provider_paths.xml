<?xml version="1.0" encoding="utf-8"?>
<paths>
    <files-path name="appPath" path="data/" /> <!-- Context.getFilesDir(). -->
    <external-path name="appFilePath" path="data/" /> <!-- Environment.getExternalStorageDirectory(). -->
</paths>